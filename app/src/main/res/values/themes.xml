<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Minimal launch theme; all in-app colors come from KliqTheme (Compose). DayNight + a
         night windowBackground (values-night) only avoid a light flash before Compose draws. -->
    <style name="Theme.KliqLoanApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/screen_bg</item>
    </style>
</resources>
